name: flutter_application1
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # SDK sürümünü kendi kurulumuna göre ayarla

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.2.1 # Veya daha güncel bir sürüm
  provider: ^6.0.0 # Veya daha güncel bir sürüm
  shared_preferences: ^2.0.0 # Veya daha güncel bir sürüm
  intl: ^0.19.0 # Veya daha güncel bir sürüm (DateFormat için)
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - images/a_dot_burr.jpeg